language: rust

rust:
  - stable
  - beta
  - nightly

matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true

cache: cargo

script:
  - find . -name *.rs | xargs rustfmt --check
  - cargo build --verbose --all
  - cargo test --verbose --all
